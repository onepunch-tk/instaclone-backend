export enum GuardRole {
  PUBLIC = 'public',
  AUTH = 'auth',
}

export enum FollowRole {
  FOLLOWED_BY = 'followedBy',
  FOLLOWING = 'following',
  ALL_FOLLOW = 'allFollow',
  UN_FOLLOW = 'unFollow',
}
